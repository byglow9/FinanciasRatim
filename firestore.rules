rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // App pessoal - autenticacao local no frontend
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
