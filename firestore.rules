rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // App pessoal - permitir todas as operações
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
