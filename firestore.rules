rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // App pessoal - exigir usuario autenticado
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
